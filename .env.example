# ── Scraper ─────────────────────────────────────────────────────────────────────
# Credentials for manodienynas.lt
# Note: The portal uses PHPSESSID which expires after ~1 hour.
# The scraper handles this by performing a fresh login on every run —
# never store or reuse a session cookie. Always use username + password here.
PORTAL_URL=https://www.manodienynas.lt
PORTAL_USERNAME=
PORTAL_PASSWORD=

# ── Supabase (Web App) ───────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # server-side only, never expose to client

# ── Notion ───────────────────────────────────────────────────────────────────────
# For the web app OAuth integration (preferred)
NOTION_CLIENT_ID=
NOTION_CLIENT_SECRET=
# Legacy: for the standalone notion.py script
NOTION_API_KEY=secret_xxxxxxxxxxxxxxxxxxxx
NOTION_DATABASE_ID=your_database_id_here
GRADES_JSON_PATH=../../scraper/grades.json

# ── Google ───────────────────────────────────────────────────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALENDAR_ID=primary

# ── Apple Calendar — no keys needed ─────────────────────────────────────────────
# .ics file is generated locally; no API credentials required.

# ── Paysera Checkout ─────────────────────────────────────────────────────────────
PAYSERA_PROJECT_ID=               # Your Paysera project ID (numeric)
PAYSERA_SIGN_PASSWORD=            # Paysera sign password / secret — server-side only
NEXT_PUBLIC_APP_URL=https://your-domain.com   # Used for Paysera callback/return URLs
