# Apple Calendar Integration

Coming in Phase 2. Generates a `.ics` file (iCalendar format) importable into Apple Calendar, Outlook, or any calendar app.

**Stack**: `icalendar` Python library — no API key needed.

**Output**: `assignments.ics` — one VEVENT per assignment due date.
