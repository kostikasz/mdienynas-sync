# Google Calendar Integration

Coming in Phase 2. Creates calendar events for every assignment due date.

**Stack**: `google-api-python-client`, OAuth2

**Env vars needed**: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_CALENDAR_ID`
